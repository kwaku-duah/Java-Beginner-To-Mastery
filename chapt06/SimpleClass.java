package chapt06;

public class SimpleClass {
    /* this is a simple class
     * keyword class followed by classname(identifier) {
     * type instance-variable (what the class has e.g width and type methodname
     * (parameters)) (what the class can do)}
     */
    double width;
    double height;
    double depth; //instance variables
}
