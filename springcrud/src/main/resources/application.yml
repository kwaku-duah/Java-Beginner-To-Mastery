spring:
  application:
    name: springcrud
  datasource:
    url: jdbc:postgresql://localhost:5432/springcrud
    username: postgres
    password: duaSHKH229
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 8082
